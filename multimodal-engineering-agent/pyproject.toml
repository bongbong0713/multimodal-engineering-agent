[project]
name = "multimodal-engineering-agent"
version = "0.1.0"
description = "A multimodal engineering data agent for point clouds, CAD, and technical documents."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
